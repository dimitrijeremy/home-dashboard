# MediaMTX configuration
# Replace the RTSP source below with your camera's RTSP URL.
# Example RTSP source: rtsp://username:password@192.168.1.100:554/stream

paths:
  frontdoor:
    source: rtsp://admin:thidm128sonny@192.168.0.20:554/cam/realmonitor?channel=1&subtype=0&unicast=true&proto=Onvif
    sourceOnDemand: yes

# RTSP listener
rtsp:
  address: :8554

# HTTP server for HLS
http:
  address: :8888
  # root: "./public"

# Logging
log:
  level: info
